<h1>用于合并txt类型的字典文件，具体功能如下 </h1>
<p>1、合并目录下所有txt文件，包括工具启动目录中的所有目录</p>
<p>2、去重所有字典，默认去重（dedupe=True），可关闭去重以加快速度。修改为：merge_txt_files(output_file="merged.txt", dedupe=False)
可以关闭去重</p>
<p>3、生成合并最终txt文件merged.txt</p>
